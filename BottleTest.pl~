#!/usr/bin/perl -w
use HipFlask;

my $Flask = HipFlask->create();
$Flask->drink();
$Flask->fill("hooch");
$Flask->drink;
$Flask->swill;
$Flask->classData;
print $ClassData."\n"."HipFlask.pm's class data (\$HipFlask->ClassData) is: $HipFlask->ClassData\n";
